{
  "name": "victorindo-kimiatama-site",
  "version": "1.0.0",
  "private": true,
  "description": "PT Victorindo Kimiatama corporate website — Eleventy + Decap CMS",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('_site',{recursive:true,force:true})\"",
    "build": "npm run clean && eleventy",
    "serve": "eleventy --serve --port=8735"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
